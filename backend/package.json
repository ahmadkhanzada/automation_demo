{
  "name": "automation-demo-backend",
  "version": "1.0.0",
  "description": "Backend API for automation demo",
  "main": "src/server.js",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "c8": "^8.0.1",
    "nodemon": "^3.0.1"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --runInBand",
    "coverage": "c8 --reporter=text --reporter=lcov jest"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
